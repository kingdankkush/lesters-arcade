# World polish candidate — 2026-09-11

This is the completed world contribution for the combined release requested by the owner. It is not a publication receipt. Integrate the frozen contribution from `Polish heroes and gameplay` before the single production promotion.

- Seven native Blender ground materials replace the noisy procedural bases: earth, red rock, wet bank, forest floor, crushed ore, industrial slab and asphalt. The committed native scene, source PNGs, recipe and receipt establish provenance; paired renders and final tile rebuilds are pixel and PNG repeatable. Seam/interior ratios are 0.894–1.060.
- Separate clipped earth paths and asphalt streets preserve authored route widths, water crossings and depth order. Natural ground colors, softened water lines, quieter wear decals and an outward-fading edge shade improve readability without changing collision or simulation.
- Twenty-seven native props in nine roadside clusters bring dressing into opening, forest and town views while preserving movement clearances and the 200-prop dressing budget.
- Optional procedural ground moved to a lazy fallback module. Twelve before/after draw traces match exactly. Missing art and explicit basic-graphics startup remain supported.
- Terrain URLs carry the v4 asset version. Service-worker v34 refreshes mutable entry files, preserves unrelated caches, bypasses no-store requests and leaves hashed chunks immutable. Reconcile the final release marker with the other contribution.

Focused validation passed: 50 terrain/provenance checks, 62 startup/fallback/cache checks, 18 decal checks and 21 final terrain-strip/production-art checks. The final world-only build uses 1,046,224 of 1,048,576 initial JavaScript bytes across 41 emitted files. Local full-suite attempts were interrupted and are not claimed as passing; the combined candidate still requires its complete release gate and hosted verification.

Actual local desktop inspection covered the opening road, reservoir, forest approach and town. The town recheck confirms the dark inner rectangle is removed. Combined desktop/mobile, hosted assets and source identity must be recorded in the final release receipt. Existing AAA, physical-device, native weapon/enemy and formal enemy-crop acceptance remain open in the retained backlog. Settlement stays disabled.
