# Parent profile and achievement continuation

## Scope

This checkpoint continues the existing mixed HMH candidate. It does not replace Codex's world, interactions, animation or three-control return, or certify a combined build. The original 55 work groups / 67 unclosed HMH-portal entries remain the acceptance inventory; this source slice does not close the whole L-5 or L-6 row.

## Changed locally

- New parent-local achievement unlock events record their first canonical UTC timestamp. Duplicate unlocks do not move it or re-award the unlock. Legacy records are not assigned an invented date.
- Profile achievements join by stable ID. Readable local-unlock dates and condition-specific progress sit beside native requirement disclosures, with semantic date/progress attributes and scoped narrow-screen/focus styling.
- Meters read only compatible existing parent aggregates. Compound, per-run or missing measurements remain explicitly unavailable; reaching a displayed threshold does not itself grant an achievement.
- The profile identifies these as device-local Testnet records, not verified chain eligibility or NFTs. Wallet separation, existing badge assets, local persistence, canonical unlock side effects and previous detailed-run-history disclosures are retained.

## Verified evidence boundary

See [machine-readable checkpoint](../qa/hmh-portal-continuation-checkpoint.json) for exact source hashes, final reviewer verdicts and raw test receipts.

- The older frozen profile-history delta received independent source review. This is not retroactive build/browser acceptance and does not automatically approve the newer achievement delta.
- Achievement/source-focused regression: 73 passed, none failed/skipped/cancelled.
- Broader source regression: 208 evaluated, 207 passed, one failing assertion for the retired `hmh-hd-sprite-atlas.png` presence contract. The failure is retained, matched to `LEGACY-TEST-RETIREMENT.json`, and is not called a green full suite.
- Six modified/new MJS files passed direct syntax checks. Added-line credential/unsafe-execution/HTML-injection scans and scoped whitespace checks passed.
- RED attempts and failed preparation attempts are retained. Source-level DOM-factory tests caught and repaired unsupported `dateTime`/progress options and direct assignment to the read-only dataset property. These tests do not replace browser interaction or accessibility-tree evidence.

## Integration instructions and remaining acceptance

1. Preserve all previous prerequisite/correction packets, the mandatory provenance supplement and the nine-file profile-history packet. This is a subsequent delta against those current local bytes, not a clean-HEAD replacement.
2. The new `apps/portal/src/achievement-progress.mjs` must be included when the canonical curated portal source inventory is regenerated. Inventory and emitted outputs were deliberately not regenerated during Codex's claimed validation window. Reconcile, regenerate and verify the actual merged module set before building; do not weaken the inventory guard or copy a stale emitted bundle.
3. After an explicit resource-window release, run inventory/build/byte gates and connected-wallet profile checks on desktop and portrait. Verify empty/partial/unlocked states, date attributes, determinate meters including zero, keyboard/touch disclosures, accessibility tree, image loads, focus targets and overflow. Also exercise the existing per-run statistics disclosures and receipt navigation through the full connected route.
4. Per-run/compound achievement progress not reconstructable from canonical aggregates remains unimplemented. Durable every-Ranked-session history, chronological build history, cross-device persistence, NFT eligibility/minting and launch-epoch reset remain separate work.
5. Codex's pending frozen return, W-10 clearance, G-7 runtime integration, full combined gates, first-time recorded playtests, physical-device acceptance, exact Preview/rollback and owner acceptance remain required before publication.

No staging, commit, push, deployment, wallet transaction, settlement activation, contract or authority change is included. `SETTLEMENT_LIVE=false` remains unchanged. STACKED and Chikun are untouched.
