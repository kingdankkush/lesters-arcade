# Investigation: destructibles

The 8 DESTRUCTIBLES are authored at apps/hmh-reboot/src/level-one-world.mjs:285-294 (id, districtId, anchor, hitPoints 80, visualKind 'destructible-cover'), exported only as LEVEL_ONE_WORLD.interactions.destructibles (level-one-world.mjs:385) and checked for unique ids by auditLevelOneWorld (level-one-world.mjs:521); they are NOT in BLOCKER_FEATURES (level-one-world.mjs:184-262), so they have no collision blocker, no nav-grid effect, no hurtbox, and no authored prop — their only consumer is world-production-art.mjs:1347-1352, which unconditionally draws a static vector crate (roundRect + cross, 18*zoom) at each anchor into layers.details. main.mjs never references interactions.destructibles (grep over apps/hmh-reboot/src: only world-production-art.mjs). The one working instance of "breakable cover hiding supplies" is the farmstead secret seal: WORLD_DESIGN_SECRET_SEAL (world-design-secrets.mjs:8) is a degenerate capsule blocker (a==b at 370,3270, radius 20, maxZ 40, combatCover) spliced into BLOCKER_FEATURES at level-one-world.mjs:187, so it lands in LEVEL_ONE_WORLD.collisionBlockers, the baked nav grid (main.mjs:4785 createEnemyNavGridChunked({world: LEVEL_ONE_WORLD})) and WORLD_BLOCKERS (main.mjs:310). Targeting: main.mjs:3427-3445 appends worldDesignSecretTargets(worldSecretState) (world-design-secrets.mjs:19-22, health = sealHealth 60) to the hurtTargets/meleeTargets loop with a fixed profile (circle r20, z 0-40); projectiles resolve against hurtTargets + WORLD_BLOCKERS (main.mjs:3578-3583) and a shot stopping on the seal blocker is converted into a damage intent by worldDesignSecretCoverHit (world-design-secrets.mjs:9-13, criticalChance 0) at main.mjs:3588-3589; melee (melee.mjs:214-219) and grenades (grenades.mjs:204-206) let a target own its own blocker id so the cover still shields anything behind it; combatTargets get the seal at main.mjs:4160; resolveCombatHits (combat-events.mjs:102, seeded crits via sessionSeed) runs at main.mjs:4202 and the seal block at main.mjs:4207-4216 copies targets[seal].health into sealHealth, and on <=0 adds the seal id to worldDesignState.openGates, rebuilds WORLD_BLOCKERS via worldDesignActiveBlockers (world-design-interactions.mjs:38-40), rebuilds the local nav neighbourhood via refreshWorldDesignGateNavigation (world-design-interactions.mjs:67-81, capsule-only, looks the gate up in world.collisionBlockers) and nulls the flow field; main.mjs:4236 skips seal damage events for run-summary/audio/flesh sparks (the cover spark still fires from the coverHit branch at main.mjs:3612-3623 using BLOCKER_VISUAL_KIND, main.mjs:313). The reward does not spawn: the supplies prop secret-prop:farmstead-hidden-supplies (assetId stacked-crates, category pickup) and the seal prop secret-seal-prop (salvage-crate, collisionBlockerId = seal id) are pre-authored in WORLD_DESIGN_SECRET_PROPS (world-design-secrets.mjs:14-17), added to siteProps at main.mjs:733 (their collisionBlockerIds join candidateBlockerIds at 734 so the graybox blocker art is suppressed), and toggled by worldDesignHiddenSecretProps (world-design-secrets.mjs:23-28) through hiddenPlacementIds at main.mjs:1486; open gates also hide graybox blocker art via nativeBlockerIds at main.mjs:1393. Collection is stepWorldDesignSecrets (world-design-secrets.mjs:29-38: sealHealth<=0, within 45 units, |dz|<=8, lineClear) consumed at main.mjs:3355-3360 which heals +30 (recordRunHealing) or refillWeaponLoadout directly and plays 'pickup'; restart at main.mjs:2851-2854 re-closes nav for every openGates id, resets WORLD_BLOCKERS and secret state. Art: the authored prop atlas (apps/portal/assets/generated/hmh-reboot-authored-props/hmh-authored-props-atlas.json) already has scrap-barricade, sandbag-nest, salvage-crate, stacked-crates, fuel-drum and loader-barrel frames usable as siteProps placements; no destructible-specific art or break animation exists. Roadmap wording: the in-repo register is REGISTER:W-12 / hmh-14 (docs/handoffs/hmh-release-reconciliation-20260911.json:210-222, docs/releases/hmh-fact-sheet.md:64) — "Finish deterministic, genuinely reachable secrets: destructible-hidden caches..." with currentDelivery claiming "Destructible supplies ... are implemented" (true only for the single seal); the literal headings "Breakable cover hiding supplies" / "Verify destructible-hidden caches" (§13/§12) are absent from this checkout (grep -ri across docs/, *.md, *.json excluding node_modules found no match).

## Key files
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/level-one-world.mjs (lines 184-262, 285-294, 264-271, 385, 521): BLOCKER_FEATURES (seal spliced at 187; destructibles absent), the 8 DESTRUCTIBLES table, COLLISION_BLOCKERS/VISIBLE_BARRIERS derivation, interactions export, unique-id audit
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/world-design-secrets.mjs (lines 8-38): Template for the generalisation: seal blocker, cover-hit to damage intent, props, state, targets, hidden-prop set, collection rule
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/main.mjs (lines 1, 310-313, 733-734, 1320, 1393, 1486, 1503-1514, 2851-2854, 3355-3360, 3427-3445, 3578-3589, 3612-3623, 3925-3933, 3986-3992, 4152-4160, 4202-4216, 4236): Every seal call site: import, WORLD_BLOCKERS/BLOCKER_VISUAL_KIND, siteProps, state, nativeBlockerIds, hidden props, telemetry, restart, reward, hurt/melee targets, projectile cover hit, cover spark, melee, grenade targets, combat targets, damage application + nav refresh, damage-event skip
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/world-design-interactions.mjs (lines 4-6, 38-40, 67-81): openGates state, worldDesignActiveBlockers, refreshWorldDesignGateNavigation (capsule-only gate lookup in world.collisionBlockers)
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/world-production-art.mjs (lines 1347-1352, 1432): Unconditional vector crate fallback for destructibles; blockerCount telemetry = world.blockers.length
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/melee.mjs (lines 214-219): Target owns its own cover blocker id (already generic)
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/grenades.mjs (lines 204-206): Blast line-of-sight treats coverHit.blockerId === target.id as clear (already generic)
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/combat-events.mjs (lines 102-145): resolveCombatHits: seeded crits, lexical target/hit ordering, targets[id].health output consumed by the seal block
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/collectible-system.mjs (lines 14-27, 29-51, 87-92): COLLECTIBLE_EFFECTS has no ammo effect; createCollectibleState throws outside 10-13 placements; entries frozen at creation
- C:/Users/just_/lesters-arcade-roadmap0911/sdk/hmh-run-summary-schema.mjs (lines 24-37): Frozen collectible effect catalogue (13 ids, schemaVersion 5) that recordRunCollectible indexes into; a new collectible effect id would ripple into the SDK schema
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/weapon-vfx.mjs (lines 24, 173-189, 200-206): IMPACT_SURFACES and COVER_SURFACE map visualKind to spark surface (destructible-cover would fall to 'dirt')
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/pickup-indicators.mjs (lines 3-6, 18): Dynamically imported marker pass reads only collectibleState.entries; needs an extra-markers input for supply drops
- C:/Users/just_/lesters-arcade-roadmap0911/apps/hmh-reboot/src/authored-prop-atlas.mjs (lines 15-19, 138-144, 902-935): Prop asset roster and createAuthoredPropDisplay.render hiddenPlacementIds path used to show/hide cover and supply props
- C:/Users/just_/lesters-arcade-roadmap0911/scripts/hmh-reboot-bundle-budget.mjs (lines 1-60): 1,048,576-byte initial JS cap including statically imported shared chunks; new simulation module must be static, presentation must be dynamic

## Gaps
- No collision blockers for the 8 destructibles: they are absent from BLOCKER_FEATURES (level-one-world.mjs:184-262), so they neither block movement nor shape enemy navigation (nav grid bakes from LEVEL_ONE_WORLD.collisionBlockers, main.mjs:4785)
- No combat targeting: main.mjs:3427 only appends worldDesignSecretTargets; no hurt target, melee target, cover-hit conversion (main.mjs:3588) or combatTargets entry (main.mjs:4160) exists for destructible ids
- No hit-point state or deterministic destruction: hitPoints 80 is dead data; no per-run state, no damage application block like main.mjs:4207-4216, no openGates/WORLD_BLOCKERS/refreshWorldDesignGateNavigation update, no restart reset
- No supply drop: no authored reward per destructible, no hidden pickup prop, no collection rule, no ammo collectible effect in COLLECTIBLE_EFFECTS (collectible-system.mjs:14-27; main.mjs:3494 'ammo-refill' branch is orphaned), no run-summary catalogue entry
- No art or break feedback: only the unconditional vector crate at world-production-art.mjs:1347-1352 (drawn even after a hypothetical break); no authored prop placement, no break burst, no debris, no accessible caption, no telemetry keys
- Geometry not validated for solids: crossing-crate-a sits 4 units from crossing-east-wreckrow and ravine-barricade-a 23 units from ravine-spur-west (would overlap/abut existing blockers), ravine-barricade-b is 58 units from the ravine-salvage-loop:4 centreline (half-width 80); all 8 are on flat walkable ground z=0 (computed via createLevelOneGroundQuery)
- Roadmap headings '§13 Breakable cover hiding supplies' and '§12 Verify destructible-hidden caches' are absent from this checkout; only REGISTER:W-12/hmh-14 wording exists (docs/handoffs/hmh-release-reconciliation-20260911.json:210-222)

## Proposed change
Follow the seal precedent exactly and generalise it to a table of N destructibles, keeping the seal untouched in phase 1 (so the seal-specific source-fragment tests keep passing).

1) New simulation module apps/hmh-reboot/src/world-destructibles.mjs (STATIC import from main.mjs and level-one-world.mjs; imports only value-guards.mjs, so no cycle):
- `export const WORLD_DESTRUCTIBLES = freezeDeep([...])` — move the 8 rows from level-one-world.mjs:285-294 here and extend each: `{ id, districtId, anchor:{x,y}, hitPoints:80, visualKind:'destructible-cover', radius:26, maxZ:48, assetId:'scrap-barricade'|'sandbag-nest'|'salvage-crate'|'fuel-drum'|'loader-barrel', supply:{ reward:'ammo'|'heal', x, y, assetId:'stacked-crates'|'bonus-life', lore } }` (supply point 40-60 units behind the cover, on ground z=0). Recommended rewards: relay-barricade-a ammo, ravine-barricade-a heal, ravine-barricade-b ammo, crossing-crate-a ammo, hashwood-deadfall-a heal, hashwood-deadfall-b ammo, mining-pallet-a heal, yard-container-lock ammo. Data-only nudges: move crossing-crate-a ~70 units north-west of crossing-east-wreckrow and ravine-barricade-a ~40 units east of ravine-spur-west; use radius 24 for ravine-barricade-b.
- `export const WORLD_DESTRUCTIBLE_BLOCKERS = WORLD_DESTRUCTIBLES.map(d => ({ id:d.id, districtId:d.districtId, anchor:d.anchor, visualKind:'containers', shape:{type:'capsule', a:d.anchor, b:d.anchor, radius:d.radius}, maxZ:d.maxZ, combatCover:true }))` — capsule because refreshWorldDesignGateNavigation (world-design-interactions.mjs:69) throws on non-capsules; visualKind 'containers' so BLOCKER_VISUAL_KIND -> COVER_SURFACE 'metal' sparks (or add `'destructible-cover':'dirt'` to weapon-vfx.mjs:173, ~40 bytes).
- `export const WORLD_DESTRUCTIBLE_PROPS = [...cover props {id:`destructible-prop:${id}`, assetId, collisionBlockerId:id, category:'environment', x, y, scale:.5}, ...supply props {id:`destructible-supply:${id}`, assetId:supply.assetId, category:'pickup', x:supply.x, y:supply.y, scale:.5}]` — same shape as WORLD_DESIGN_SECRET_PROPS (world-design-secrets.mjs:14-17).
- `export function createWorldDestructibleState()` -> `{ health:new Map(ids -> hitPoints), brokenTick:new Map(), collected:new Set(), lastTick:-1, lastNotice:null }`.
- `export function worldDestructibleTargets(state)` -> intact entries as `{id, x, y, previousX, previousY, groundZ:0, previousGroundZ:0, radius, health, maxHealth:hitPoints, active:true}` (lexical id order), mirroring world-design-secrets.mjs:19-22.
- `export function worldDestructibleHurtProfile(id)` -> `{bodyShape:{type:'circle',radius}, projectileShape:{type:'circle',radius}, meleeRadius:radius, minZ:0, maxZ}` or null.
- `export function worldDestructibleCoverHit({resolution, shot, tick})` -> generalised world-design-secrets.mjs:9-13: if `resolution.coverHit?.blockerId` is a destructible id and no `resolution.hits` entry already targets it, return `{id:`cover:${shot.id}`, tick, time, targetId, sourceId:'player', weaponId, damage:shot.damage, criticalChance:0, criticalMultiplier:1, direction:{x:shot.vx,y:shot.vy}, knockback:0, point}`; else null.
- `export function applyWorldDestructibleDamage(state, {targets, tick})` -> for each intact id present in `targets` (lastCombatResolution.targets), set health; when <=0 set brokenTick; return `[{id, tick, supply, anchor}]` sorted by id.
- `export function stepWorldDestructibleSupplies(state, {tick, player, queryGround, lineClear})` -> monotonic-tick guard; for each broken, uncollected entry: within 45 of supply point, |player.groundZ - queryGround(...).groundZ| <= 8, lineClear -> add to collected, set lastNotice, emit `{id, reward, name, lore, tick}` (mirrors world-design-secrets.mjs:29-38).
- `export function worldDestructibleHiddenProps(state)` -> Set: intact => hide `destructible-supply:${id}`; broken => hide `destructible-prop:${id}`; collected => hide supply too.

2) level-one-world.mjs: `import { WORLD_DESTRUCTIBLES, WORLD_DESTRUCTIBLE_BLOCKERS } from './world-destructibles.mjs'`; insert `...WORLD_DESTRUCTIBLE_BLOCKERS` after WORLD_DESIGN_SECRET_SEAL at line 187; delete the local DESTRUCTIBLES const (285-294) and keep `interactions: { destructibles: WORLD_DESTRUCTIBLES, ... }` at 385 so existing tests and world-production-art keep working. COLLISION_BLOCKERS/VISIBLE_BARRIERS/BLOCKERS derive automatically, so auditCollisionWorld, minimap hardBoundaries and dataset.worldBlockers stay consistent.

3) main.mjs call sites (all inside the fixed-step simulation, no RNG):
- line 1 area: import the new functions/constants.
- 733: `const siteProps = [...WORLD_DESIGN_SITE_PROPS, ...WORLD_DESIGN_ORCHARD, ...WORLD_DESIGN_SECRET_PROPS, ...WORLD_DESTRUCTIBLE_PROPS];` (cover props carry collisionBlockerId, so 734 adds them to candidateBlockerIds and the graybox blocker art is suppressed once the atlas is ready).
- 1320: `let worldDestructibleState=createWorldDestructibleState();` and after 2854: `worldDestructibleState=createWorldDestructibleState();` (append AFTER the `worldDesignState=createWorldDesignState();` line so the restart regex in tests/hmh-world-design-interactions.test.mjs:83 still matches; the existing `for(const gateId of worldDesignState.openGates)` loop at 2851 already re-closes broken destructibles because they are recorded in openGates).
- 3427-3431: iterate `[...grayboxEnemies,...worldDesignSecretTargets(worldSecretState),...worldDestructibleTargets(worldDestructibleState)]` and resolve `profile = worldDestructibleHurtProfile(enemy.id) ?? (enemy.id===WORLD_DESIGN_SECRET_SEAL.id ? sealProfile : createOrdinaryEnemyHurtboxProfile(enemy.radius))` — keep the literal fragment `...worldDesignSecretTargets(worldSecretState)`.
- 3588-3589: add `const coverImpact=worldDestructibleCoverHit({resolution,shot,tick}); if(coverImpact)combatHitIntents.push(coverImpact);` (melee 3925-3933 and grenades 3986-3992 need nothing: they already receive meleeTargets/hurtTargets and treat coverHit.blockerId===target.id as own cover).
- 4160: `combatTargets.push(...worldDestructibleTargets(worldDestructibleState));`
- after 4216: `for(const broken of applyWorldDestructibleDamage(worldDestructibleState,{targets:lastCombatResolution.targets,tick})){ worldDesignState.openGates.add(broken.id); WORLD_BLOCKERS=worldDesignActiveBlockers(worldDesignState,LEVEL_ONE_WORLD.collisionBlockers); refreshWorldDesignGateNavigation(ENEMY_NAV_GRID,LEVEL_ONE_WORLD,queryGround,broken.id,WORLD_BLOCKERS); enemyFlowFieldTick=-1; enemyFlowField=null; pushCombatVisualEvent({type:'cover-break',tick,point:{...broken.anchor,z:queryGround(...).groundZ},surface:'metal'}); combatAudio.play('enemy-hit',{volume:.14}); if(settings.captionCriticalAudio) setAccessibleCombatStatus('Cover destroyed. Supplies exposed.'); }`
- 4236: extend the skip to `|| worldDestructibleState.health.has(damageEvent.targetId)` (precompute `DESTRUCTIBLE_IDS` Set) so cover damage never feeds recordRunDamage/enemy-hit audio/flesh sparks; the per-hit cover spark at 3612-3623 already fires for the blocker contact.
- after 3360: supplies loop identical to the secrets loop: `for(const supply of stepWorldDestructibleSupplies(worldDestructibleState,{tick,player:actor,queryGround,lineClear:<same traceHeightAwareLineOfSight closure>})) { heal +30 via recordRunHealing | refillWeaponLoadout(weaponLoadout,{tick,progressionByWeapon}); combatAudio.play(reward==='heal'?'health-pickup':'ammo-pickup',{volume:.11}); setAccessibleCombatStatus(...) }`. Rewards therefore bypass collectible-system on purpose: createCollectibleState throws outside 10-13 placements (collectible-system.mjs:30), the collectible smoke pins collectibleRemaining===13 (scripts/hmh-reboot-collectible-browser-smoke.mjs:58), there is no ammo effect in COLLECTIBLE_EFFECTS, and a new effect id would have to be added to the frozen SDK catalogue sdk/hmh-run-summary-schema.mjs:24-37 (schemaVersion 5 pinned at tests/hmh-reboot-run-summary.test.mjs:99). If the collectible-system route is still wanted later: add `hidden:true` placements + `revealCollectible(state,id,tick)`, exclude hidden entries from remainingCount, raise the placement bound to 21, reuse effect 'bonus-life' for heal and add `'stacked-crates': {effectId:'ammo-cache', kind:'ammo-refill'}` plus the catalogue/schema bump.
- 1486: add `...worldDestructibleHiddenProps(worldDestructibleState)` to hiddenAuthoredPropIds.
- 1503: pass `secretState:{lastNotice: newest of worldSecretState.lastNotice / worldDestructibleState.lastNotice}` so world-design-life.mjs:126-132 shows the supply caption.
- 1512-1514: `dataset.worldDestructiblesBroken=JSON.stringify([...worldDestructibleState.brokenTick.keys()]); dataset.worldSuppliesCollected=JSON.stringify([...worldDestructibleState.collected]);`
- pickup marker: extend pickupIndicators (dynamic module) with an optional `extraPickups:[{id,x,y,effectKind}]` argument built from broken-but-uncollected supplies at 1481.
- world-production-art.mjs:1347: skip the fallback crate when `nativeBlockerIds.has(destructible.id)` (native prop ready or broken), so the vector crate disappears on break even in fallback art.

4) Projection-only break burst: new apps/hmh-reboot/src/cover-break-presentation.mjs loaded via `import()` beside gore-presentation (main.mjs:1199 pattern): `createCoverBreakPresentation()` -> `{add(event, groundZ), render({camera,view,worldToScreen,tick,reduceMotion,particleBudget}), clear()}`; on 'cover-break' spawn <=12 debris chips + a dust ring for 30 ticks using impactSprayAngles (combat-feedback.mjs:48) seeded by `${id}:${tick}`; emit nothing when settings.reduceMotion or performanceProfile.particlesPerHazard===0; reads only the frozen event. Clear it in the reset block at 2801.

Determinism/projection split: everything in world-destructibles.mjs and the main.mjs simulation edits is a pure function of tick, authored data, WORLD_BLOCKERS and resolveCombatHits(sessionSeed); the burst, markers, captions and dataset keys never feed back.

Determinism risk: low. All new state is tick-driven and mirrors the existing seal/gate path: damage comes from resolveCombatHits (combat-events.mjs:102) with seeded crits (cover-hit intents use criticalChance 0; melee 0.08 is seeded per hit id), break order is fixed by lexical id sort, blocker removal + refreshWorldDesignGateNavigation + flow-field reset reproduce main.mjs:4210-4215, and supply collection is proximity/height/LOS over WORLD_BLOCKERS like stepWorldDesignSecrets. Breaking cover changes WORLD_BLOCKERS and enemy navigation mid-run, so it is replay-affecting but deterministic; the module must be a static import (simulation) while the burst stays dynamic. Risks: (a) destructible blockers must be capsules or refreshWorldDesignGateNavigation throws; (b) the damage block must sit at the same point in the tick as the seal block (after resolveCombatHits, before enemy health application) so ordering is identical across runs; (c) the restart loop regex (tests/hmh-world-design-interactions.test.mjs:83) constrains where the new reset line goes. No HMH same-seed replay fixture file exists to regenerate: tests/fixtures holds stacked-golden-run.json and stacked-downkick-counterexample.json (STACKED) and hmh-ground-fallback-reference.json (ground fallback); hmh-run-integrity.test.mjs:142-160 hashes a replay envelope over run metadata that this change does not touch unless the run-summary catalogue is edited.

Bundle impact estimate: 4600 B. Effort M. Owner value 4/5.

## Tests to add
- tests/hmh-world-destructibles.test.mjs: every WORLD_DESTRUCTIBLES id has a capsule blocker in LEVEL_ONE_WORLD.collisionBlockers with combatCover true, sits on walkable ground z=0, keeps >= 24+radius clearance from every route centreline (routeGraph.edges) and is not inside the protected spawn disc (560+24)
- tests/hmh-world-destructibles.test.mjs: a projectile that stops on each destructible blocker converts into exactly one damage intent via worldDestructibleCoverHit (no duplicate when resolution.hits already names the id; null for other blockers), and melee/grenade damage the cover while an enemy behind it is still shielded (port of tests/hmh-world-design-secret-combat.test.mjs for all 8 ids)
- tests/hmh-world-destructibles.test.mjs: two runs of resolveCombatHits with the same sessionSeed and hit sequence break the same id at the same tick; 80 hp requires the same number of coin-blaster hits; applyWorldDestructibleDamage returns lexically ordered break events and never re-breaks
- tests/hmh-world-destructibles.test.mjs: breaking adds the id to openGates, worldDesignActiveBlockers drops it, refreshWorldDesignGateNavigation changes walkable cells around the anchor, and the exact main.mjs restart fragment (vm.runInNewContext like tests/hmh-world-design-interactions.test.mjs:70-89) re-closes them and resets worldDestructibleState
- tests/hmh-world-destructibles.test.mjs: supplies are hidden while intact, exposed after break, require same-height contact within 45 and clear line of sight, collect once per run, and reset with a new state; worldDestructibleHiddenProps toggles cover/supply prop ids correctly through intact -> broken -> collected
- tests/hmh-world-destructibles.test.mjs: source-fragment assertions that main.mjs wires `...worldDestructibleTargets(worldDestructibleState)`, `worldDestructibleCoverHit({resolution,shot,tick})`, `applyWorldDestructibleDamage(worldDestructibleState`, `stepWorldDestructibleSupplies(worldDestructibleState`, `worldDestructibleHiddenProps(worldDestructibleState)` and `worldDestructibleState=createWorldDestructibleState()` after the worldDesignState reset
- tests/hmh-world-destructibles.test.mjs: nav reachability from the player spawn to every supply point with all blockers intact (createEnemyNavGrid + computeEnemyFlowField, pattern of tests/hmh-world-design-secrets.test.mjs:27-33) and unchanged reachability for WORLD_DESIGN_SECRETS and WORLD_DESIGN_SITES
- tests/hmh-reboot-cover-break-presentation.test.mjs: the dynamically imported burst emits bounded particles per event, zero under reduceMotion or particleBudget 0, reads nothing from simulation state, and clears on reset
- extend tests/hmh-reboot-world-production-art.test.mjs: the fallback vector crate is skipped for ids in nativeBlockerIds (broken or natively dressed)
- extend scripts/hmh-reboot-combat-browser-smoke.mjs: shoot relay-barricade-a from the relay entry, assert dataset.worldDestructiblesBroken lists it, dataset.projectileCover named it before the break, and dataset.worldSuppliesCollected updates after walking to the supply point; re-run node build.mjs to confirm the initial JS budget gate

## Existing tests that constrain
- tests/hmh-world-design-secrets.test.mjs:9-13 — main.mjs must keep the literal fragments `...worldDesignSecretTargets(worldSecretState)`, `lastCombatResolution.targets[WORLD_DESIGN_SECRET_SEAL.id]`, `stepWorldDesignSecrets(worldSecretState`, `worldSecretState=createWorldDesignSecretState()`, `worldDesignHiddenSecretProps(worldSecretState)` (do not fold the seal into the new module in phase 1)
- tests/hmh-world-design-secret-combat.test.mjs:18 — fragment `worldDesignSecretCoverHit({resolution,shot,tick})` must remain
- tests/hmh-world-design-interactions.test.mjs:70-89 — regex over main.mjs:2851-2853 (`for(const gateId of worldDesignState.openGates)...WORLD_BLOCKERS=...worldDesignState=...;`) is executed in a vm context; the new reset line must come after it and the loop must still reference only those symbols
- tests/hmh-world-design-secrets.test.mjs:27-33 — nav reachability from spawn to every secret with gates+seal removed; new blockers must not sever those paths
- tests/hmh-world-design-routes.test.mjs:28-40 — radius-24 actor traverses every authored route segment; ravine-barricade-b (58 units from ravine-salvage-loop:4 centreline) must keep radius <= 26 or move
- tests/hmh-world-design-opening-clearance.test.mjs:24-36 — solid footprints must stay outside the protected spawn disc plus actor margin (relay-barricade-a is 971 units away: fine)
- tests/hmh-reboot-level-one-world.test.mjs:152-165, 167-175, 189, 208 — destructibles >= 8 with unique ids across POIs/arenas/explosive zones, auditCollisionWorld ok, minimap hardBoundaries and reveal alwaysVisibleBoundaryIds must equal visibleBarriers (auto-derived, but blocker ids must stay unique against the existing 60+ blockers)
- tests/hmh-reboot-level-one-composition.test.mjs:88 — blockers >= 26 (unaffected)
- tests/hmh-world-render-lifecycle.test.mjs:136-150 — executes the main.mjs authored-prop load arrow in a synthetic vm context that whitelists WORLD_DESIGN_SECRET_PROPS/WORLD_DESIGN_SITE_PROPS/WORLD_DESIGN_ORCHARD; adding WORLD_DESTRUCTIBLE_PROPS to siteProps (main.mjs:733) requires adding it to that context
- scripts/hmh-reboot-combat-browser-smoke.mjs:218,333,364 — dataset.worldBlockers must equal LEVEL_ONE_WORLD.collisionBlockers.length (auto-consistent via world.blockers.length at world-production-art.mjs:1432)
- scripts/hmh-reboot-collectible-browser-smoke.mjs:58 and tests/hmh-reboot-collectible-system.test.mjs:18-30 — collectibleRemaining pinned at 13 and placements at 10; this is why supplies should not be added as collectible-system placements without a contract change
- tests/hmh-reboot-run-summary.test.mjs:99 and sdk/hmh-run-summary-schema.mjs:24-37 — schemaVersion 5 and the frozen collectible catalogue; only touched if a new collectible effect id is introduced
- tests/hmh-reboot-world-production-art.test.mjs:121 — layer list is pinned; the vector-crate gating must not add a layer
- tests/hmh-reboot-enemy-navgrid.test.mjs, tests/hmh-finite-map-spawn.test.mjs, tests/hmh-bounded-world-ai.test.mjs — re-run after adding blockers to confirm spawn-point and arena reachability (no pinned walkable counts found by grep, but routing assertions exist)
- node build.mjs budget gate (scripts/hmh-reboot-bundle-budget.mjs) — 18.7 KB headroom; the static simulation module (~2.4 KB min) + main.mjs edits (~1.7 KB) + world data (~0.4 KB) must stay under it
