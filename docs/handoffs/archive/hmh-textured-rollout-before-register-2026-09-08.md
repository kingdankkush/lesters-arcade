# Textured rollout: current continuation checkpoint

## Current checkpoint: owner decision on further hero hardening

This section supersedes status claims below. Preserve all source masters and prior evidence. No textured rollout is committed, release-certified or live.

- **Local four-hero milestone remains intact:** all four named heroes have textured, rigged, nine-action gameplay atlases and matching canonical selectors. The earlier exact local build passed 20 viewport profiles and real movement/firing recordings; its evidence is `.tmp/team/hero-local-milestone.json`. That browser result predates the latest guard corrections.
- **Second automatic correction cycle is complete.** The third-context implementer returned seven source/test/config files; parent verified hashes. A real selector refresh passed with native sources unchanged: `.tmp/team/selector-final-receipt.json`. The current selector `--check` passed. Parent's fresh runtime plus Python-selector wrapper passed; the broader 171-test intake run had one active enemy-kind integration failure, forwarded to that worker, not waived.
- **Further hero-code edits are PAUSED pending owner direction.** Both final source reviewers returned remaining findings after two correction cycles. Parent verified the frozen source hashes and reproduced the fractional logical-pivot rejection without modifying any asset. Read `.tmp/team/review-final/parent-adjudication.json` and `runtime-pivot-witness.json`. The proposed additional bounded scope is coordinate tolerance, private drift-report routing, direct exporter destinations, measured pitch, private quality caps and runner provenance. The request for duplicate production-source authority declarations was not established against the actual schema and must not be blindly implemented.
- The review packet ledger recorded an LF-content digest before Windows newline translation, not the raw stdin-file digest. The adjudication preserves original records and separately records both hashes; do not mislabel them as identical. Source-file snapshot hashes match. Both CLI review processes exited normally; their only non-message item was the recognized shortened-skill-description metadata warning, not a command/tool execution.
- **Native enemy implementation handback verified:** `proc_f2c9fec54536` completed without adopting canonical art or building. Parent rehashed 23 source files, all 22 existing enemy artifacts and four candidate outputs, replayed 55 focused Node tests with 12 nested Python tests, and exercised three actual adoption-preflight corruption challenges. Other canonical actors remained untouched. `.tmp/team/bagholder-integration/parent-verification.json` is the parent receipt, not an adoption certificate.
- The .50 rusher calibration measures .8392529519687633 of median hero height, with unchanged ordinary/boss limits and simulation. A separate current-source build under `.tmp/team/bagholder-parent-preview/portal` passed. Browser technical checks passed desktop/portrait/short-landscape with real keyboard input, normal HUD, exact served bytes and no minimap; **visual acceptance is desktop-only**. Mobile native-subject framing/help occlusion still fails. See `visual-adjudication.json`; `smoke-v4.mjs` is prepared but UNEXECUTED. The private listener `proc_7c9d65da9f25` has been stopped. Do not reuse its origin as live.
- **Native hit peak is a real remaining blocker:** a read-only Blender probe kept source/actions unchanged and found HMH_Enemy_Hit frames 1..25 peak at frame 12, while the exported samples [1,25] reach only .2785323432304823 of the measured peak. See `hit-source-probe.json`; coverage/unique hashes do not certify hit readability.
- Independent enemy runtime review passed its frozen scope. The pipeline review did not: false paired-provenance in unverified modes, insufficient reuse/uniqueness checks, raw-preservation overclaim and provenance-content validation need correction. This does not reopen the separate hero hold.
- **Active enemy correction, cycle ONE:** `proc_d9fd4a037a0d`, brief/status under `.tmp/team/bagholder-correction/`. It owns only the listed enemy corrections plus explicit hit peak/recovery sampling, preserving the native Blend/action curves, 152 IDs, FPS, .50/.75/.86 scales, 2048/2MiB and 8/2/32 limits. Last observed state: real sequential A/B after 21 Python tests and source probe [12,25] green; these remain worker-reported pending parent verification. Parent owns new-candidate adoption, build, browser/hit/collision proof and release. Do not run a competing GPU/browser job until its render ownership returns.

Do not start another automatic hero fix loop from a delayed notification. Await an explicit owner decision, preserve the six-item bounded scope, and do not restart modeling. Continue only independently authorized non-hero work. Final release still requires exact scoped review, complete gates, fresh canonical player/selector flow, current-build browser/fault/performance/endurance proof, clean no-Git Node24 build, verified promotion and rollback.

## Scope and owner authority

Continue this worktree, not the older pilot. Branch: `hermes/hmh-textured-rollout`; base commit: `9ce7372021a1b14a5b6910bcd0fa1d7d328d8bef` (reference-accurate selector biographies). All current staged/unstaged game changes in this newly created worktree are this rollout's work. Preserve the old worktrees and original native masters.

The owner renewed the instruction to complete playable heroes, enemies, weapons, gameplay/controls/movement/combat, world/level art, power-ups, particles, blood/gore and sound, then publish. Exact approval: “You have my approval on everything to work all tasks to completion. Then push everything live.” This authorizes verified **game/website-layer** delivery; it does not waive failed gates or authorize contracts, funds, credentials, authority changes or settlement. Keep `SETTLEMENT_LIVE=false`, fixed 60 Hz, current schemas/IDs and parent authority. Heroes first, then enemies, then world; do not divert into STACKED.

No update from this candidate has been pushed/promoted. No current candidate has a final release certificate. Do not report the program, four heroes, or production as complete.

## What exists and was exercised

- Preserved Commando source/atlas integrated onto the current defect-fix lineage. Lossless WebP, schema-2 orig/trim handling, native pistol/melee/grenade routing and 648-frame coverage. Original pilot remains untouched.
- Corrected loading validation: exact contiguous required clips, approved native route IDs, strict LFS pointer identity; safe readable fallback with camera composition. All living body poses contribute to the 12% minimum; flat death corpses are deliberately excluded from that framing minimum.
- Local normal-HUD browser matrix passed desktop, ultrawide, tablet, portrait and landscape. Independent fault injection (missing frame, unapproved native weapon) passed desktop and portrait: fallback remained visible, simulation advanced, real keyboard/touch input moved it, no uncaught errors. Source and browser evidence are separate, not production proof.
- Native transfers for Valkyrie v22, Lilly v93 and Lester Original v29 were compared against their actual masters: all three semantic fingerprints matched. Native checker also detected seven deliberate in-memory changes (skin weight, topology, material assignment, object transform, armature modifier, action channel, bone bind). It never saved those mutations.
- Native action-polish pipeline exercised on Valkyrie: five actions changed (breath/recoil/anticipation/follow-through/death onset), four unchanged including run. Geometry, UVs, skin/rest rig/materials remained identical after native reopen; 125 source frames grounded, maximum residual `0.00004850700497627258` m.
- Weighted Valkyrie final source is `.tmp/textured-rollout-sources/Lit Valkyrie - Weighted Gameplay compact-v2.blend`, SHA-256 `f090f2e2a387f8ee3c2666bed1af22344f6ec82b1695bc62f1d18f1cfb8268ec`, 61,129,265 bytes. Zstandard container recompression preserved the entire decoded Blend byte stream. A prior compact-v1 was successfully opened/used in Blender; each final compact source still needs its own renderer/native readback.
- Explicit released-prop visibility now spans expected frames, analysis, packing, runtime validation and decoded image QA. Only weapon/grenade slots 3 and 4 can be canonical transparent 1x1 cells. All 648 IDs remain required; visible animated count is 624 when those 16 cells are present, otherwise 640. Blank body art, arbitrary visibility, visible “null” cells, incomplete direction coverage and ordinary duplicate art remain failures.

## Exact current blockers, not owner questions

1. Final selector regeneration is outstanding. Current static GLB turntables' `sources.heroExporterSha256` is stale after the actual exporter correction. Two release-test failures correctly expose this. **Do not hand-edit/bless provenance hashes.** Regenerate selectors from the final same gameplay sources, or actually rerender the existing source if doing a narrower release. Prefer the requested same-source completion.
2. The source gate run `.tmp/textured-core-source-gates-1/receipt.json` passed check/security/health/build, but release failed. Its two old graybox-construction regex failures were subsequently corrected and focused-tested; the two selector-provenance failures remain. Do not expand the retirement ledger to hide them.
3. Valkyrie v3 completed real A/B rendering and passed non-shadow uniqueness after the motion fix, but the private **zero-tolerance experiment** found one blue-channel value differing by one at `(129,154)` in `lower-body/dash/west/001`. Canonical repository policy is and remains `maxChangedVisiblePixels=8`, `maxChannelDelta=2`, `maxTotalChannelDelta=32`, with mode `bounded-premultiplied-rgba-v1`. The override-zero diagnostic is retained, not a changed production threshold. `.tmp/textured-rollout-weighted-v4-manifest.json` restores that exact existing canonical policy. Report actual deltas; never call nonzero output bit-identical. No production threshold was raised.
4. The full-pipeline visible-frame count was subsequently corrected for the 16 legitimate released-prop null cells. Fresh final pipeline execution is still needed after that correction.
5. Gameplay/source/selector bindings for Valkyrie, Lilly and Lester are not yet shipped. Runtime native allowlists still intentionally approve only Commando. Expand per actor only with its verified binding; never trust fetched metadata to grant support.
6. Final exact-candidate independent review, clean no-Git Node 24 host build, visual regression, canonical active-combat endurance and public readback are outstanding.

## Active / durable artifacts

All paths below are relative to this worktree unless specified.

- `.tmp/remaining-weighted-source-batch.json` and `.tmp/remaining-weighted-source-batch.log`: sequential native polish and lossless compaction of Lilly and Lester; launched as process `proc_908faf7ebc0c`. Read actual status before another Blender job. Count must be exactly two distinct actors and `finished=true`; do not infer completion from file existence.
- `.tmp/native-transfer-proof.json`: three original-to-derived semantic matches, finished true.
- `.tmp/native-preservation-green.json`: seven real mutation challenges, passed. Native challenge process exited zero.
- `.tmp/textured-rollout-sources/`: original derived v1 scenes, weighted v2 scenes, compact scenes, `.preparation.json`, `.motion.json`, and `.container-proof.json` receipts. Originals elsewhere are immutable.
- `.tmp/textured-rollout-valkyrie-v3/`: real raw run A/B frames and reports; not a final accepted export. `.tmp/valkyrie-repro-diagnostic.json` and `valkyrie-repro-policy-disposition.json` preserve the experiment.
- `.hermes/evidence/textured-rollout-valkyrie-source/weighted-nine-pose-eight-direction.png`: source contact sheet inspected, including death details. Same human/anatomy connected; runtime/timing review still required.
- `.hermes/evidence/hmh-reboot-phase20-production-hero/`: latest local Commando five-profile pictures. `.hermes/evidence/hmh-textured-fallback/report.json`: four fault-injected browser cases.
- `.tmp/commando-review/final.json`: original frozen patch `4a7f2924fd9aeb27ea2d00353bc0461aa97627ec0b7f23f2e95d436891e5d433` FAILED independent review. Corrections are real and tested, but no fresh final PASS exists.
- `.tmp/review-correction-a`, `review-correction-b`, `review-correction-native-delta`: independent command-free corrective proposals/events and application receipts. Recognized skill-description budget warnings are retained; zero tool/command events and completed turns were verified.
- `.tmp/textured-core-candidate-1.json`: intermediate index hash `98456aba0e5ae86cdbc885d68950a95949e29e6d4a8c463f24fab8fde48bc3f0`, **superseded by later expected-count/test fixes**. Re-freeze; do not reuse it as final.
- `.tmp/run-textured-core-source-gates.py`: durable Node 24.20.0 runner. Use a new output folder/run receipt when repeating, rather than erase previous evidence.

## Immediate execution order

1. Verify completion of the remaining two native source jobs. Preserve failures/partial outputs. Add their actual final compact paths/hashes to a new private weighted manifest. All three now derive from the supplied completed native masters, not re-generated Tripo jobs.
2. Run final paired gameplay exports under the unchanged canonical render budget. Verify every report, 648 IDs, visible/hidden counts, distinct visible poses, exact lossless decoded reconstruction, 2048 dimensions, 4 MiB per hero / 16 MiB combined compressed payload, and native source caps. Inspect poses at gameplay distance before binding.
3. Bind each verified textured hero and source provenance onto this lineage with TDD. Preserve stats/unlocks, native-only supported weapons/actions, fallbacks, same-scale enemies, collision and deterministic authority. Record source vs runtime vs production separately for all four.
4. Complete same-source selectors. Existing files to read: `scripts/run-hmh-hero-selector-render.py`, `scripts/hmh-blender/export-hmh-hero-selector.py`, `apps/hmh-reboot/assets/source/blender/hmh-hero-selector-render.json`. Current mode is `static-textured-models` using four old unrigged GLBs and a combined scene. A per-hero packed-gameplay-source mode avoids a huge merged scene and action-name collisions: open each immutable native model read-only, bind its real aim/idle action, render turntable directions, preserve all input hashes, and teach Blender-free provenance checking the actual per-hero source list. This is a proposed direction, **not implemented**. Keep PNG/selector budgets unless a separately tested lossless-format extension preserves them. Improve dark/under-filled presentation with real visual evidence, not hash-only updates.
5. Run full applicable gates, fix actual failures, freeze an exact patch and obtain independent review. Native delegation's configured free MiniMax is unavailable; use installed Codex command-free source packets in bounded file groups. Never retry a denied command with broader policy. Source-only review is not visual acceptance.
6. Clean no-Git/pointer-only Node 24 host build, actual portal selector-to-game flows at five profiles, canonical performance/endurance and visual-baseline certification. Never run competing browser/GPU jobs or rebuild served output during a performance window.
7. Publish only verified game-layer candidates using the repository's branch-preview/promotion workflow; verify exact remote SHA, deployment/alias IDs, public artifact bytes and real browser gameplay, recording rollback. Do not promote failed/WIP output.
8. Continue the remaining granular backlog and the owner's requested enemy/weapon/world/gameplay/effects/audio program in dependency-ordered, verified slices. Use the supplied gameplan review and `GAMEPLAN-2026-09-07-RECONCILIATION.md/.json`; do not equate this core work with closing all items.

## Continuation worker launch: approval-blocked checkpoint

- Observed the required worktree and `hermes/hmh-textured-rollout` at `9ce7372021a1b14a5b6910bcd0fa1d7d328d8bef`; preserved all staged/unstaged rollout work. Wrote `.tmp/continuation-worker-started.json` and `.tmp/continuation-worker-status.json`. Session ID was not available from successful tool output.
- Read the remaining weighted-source receipt: it reports `finished=true`, `expectedCount=2`, with distinct Lilly and Lester Original entries, decoded-byte equality and source-cap checks true, and native reopen pending for both. These are receipt claims, not this worker's independent hash/native verification.
- Polling the supplied `proc_908faf7ebc0c` handle returned `not_found`. This does not prove the native process is finished or absent. Do not start an overlapping Blender job on that basis.
- A combined Git-root/environment/native-process inspection command was rejected by terminal approval enforcement: `script execution via -e/-c flag`, with approval unavailable in single-query mode. The rejected operation was not retried through another tool, script wrapper, permission mode, or configuration change. No Blender export, runtime binding, certification, commit, push or deployment occurred in this worker.
- Resume requires an approved way to execute the blocked prerequisite inspection in an interactive/approval-capable launch, not a bypass. Then verify actual native-process status and compact-file hashes before creating the next private weighted manifest and running the final real paired exports. Existing selector and certification blockers above remain unchanged.

## Operational notes

- Preserved old pilot: sibling `lesters-arcade-cycle081-tripo-gameplay`; never wipe or reset it. Original completed masters reside in sibling `lesters-arcade-cycle078-rig-pilot` under source/models/tripo. `.tmp/native-transfer-inputs.json` has verified exact original paths/hashes.
- A local server was launched for `apps/portal` on `http://127.0.0.1:8973`. Verify sentinel responses before reuse. Real browser scripts take `HMH_REBOOT_ORIGIN` (use `MSYS_NO_PATHCONV=1` under Git Bash).
- `scripts/hmh-reboot-production-hero-browser-smoke.mjs --actor <id>` and `scripts/hmh-reboot-hero-fallback-browser-smoke.mjs` are registered. Latest fallback script should be rerun after its promotion from the ignored probe.
- Python is 3.11; `python3` is 3.14.5 with `compression.zstd`. The lossless native-container compaction utilities are private, not CI dependencies. Native Blender is `D:/Apps/Blender/blender.exe` 5.1.2. Explicit `--disable-autoexec` precedes a loaded Blend, and `--python-exit-code 1` protects native jobs.
- No cron/gateway/config changes were made. Do not start unrelated schedulers or modify other Hermes profiles.
