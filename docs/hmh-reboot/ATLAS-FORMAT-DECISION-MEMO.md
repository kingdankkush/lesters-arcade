# Hero atlas format decision memo (P-6, AAA-ROADMAP 8.3.2)

Status: **measurement complete, owner decision pending**. Nothing about the hero atlas
format ships until the owner answers in chat and the answer is recorded in `DECISIONS.md`
(repo root). Generated by `scripts/hmh-hero-atlas-format-harness.mjs` on 2026-09-05T07:46:56.062Z
against base commit `90a744d4`; no shipped asset or runtime file was changed by the run.

## The question

Before the 256 px heroes (C-1, C-3) land, pick the shipping format for the four hero atlases.
AAA-ROADMAP 8.3.2 lists four options: **raise the 12.6 MB (12 MiB) four-hero cap**, **adopt
lossless WebP**, **adopt KTX2**, or **load one hero at a time**. The roadmap's standing
recommendation is per-hero lazy loading plus WebP unless KTX2 wins on mobile GPU memory.

## Facts on the base commit

- Per-hero atlas cap `3,407,872` bytes (3.25 MiB) and four-hero total cap
  `12,582,912` bytes (12 MiB), asserted by
  `scripts/hmh-reboot-production-asset-qa.mjs`, which decodes atlases as PNG only.
- Shipped atlases are 2048x2048 RGBA, 648 frames each, 160x160 source frames trimmed and
  packed; visible pixels are about 33.5% of the sheet.
  - `lester-original`: 3,242,471 bytes (95.1% of the per-hero cap)
  - `lilly`: 3,082,625 bytes (90.5% of the per-hero cap)
  - `lit-commando`: 3,002,530 bytes (88.1% of the per-hero cap)
  - `lit-valkyrie`: 2,951,722 bytes (86.6% of the per-hero cap)
  - four-hero total 12,279,348 bytes = 97.6% of the 12 MiB cap (303,564 bytes of headroom).
- **Per-hero lazy loading is already the live behaviour**: `apps/hmh-reboot/src/main.mjs`
  loads exactly one hero atlas (the selected `productionHero`, default `lit-commando`) on
  boot and swaps it in on decode. The 12 MiB total bounds hero switching over a session, not
  boot. Option four is therefore already done and the question is really format plus page
  count plus the per-hero cap.
- The shipped Pixi 8.19.0 vendor chunk already accepts `.webp` and `.avif` URLs through
  `Assets.load` (createImageBitmap path); no JavaScript changes for WebP. KTX2 needs
  `import 'pixi.js/ktx2'` (about +14 KB in the vendor chunk) plus a self-hosted transcoder
  (`libktx.js` 216,472 + `libktx.wasm` 713,727 bytes) because `vercel.json` CSP
  `script-src 'self'` blocks Pixi's default jsDelivr transcoder URL.
- Bundle headroom on the base commit is 29,941 bytes of 1,050,000; WebP costs 0 bytes there.

## Method

Measured hero: `lester-original` (3,242,471 bytes, the largest of the four, 95.1%
of the per-hero cap). Conversion by Pillow 12.3.0 (libwebp 1.6.0) on
Python 3.11.16: lossless WebP uses `exact=True` because Pillow's default rewrites RGB
under alpha 0 and would break the per-frame RGBA hash pins even though no visible pixel
changes; lossy uses quality 90, method 6, alpha untouched.

Browser timings come from headless Chrome (`C:\Program Files\Google\Chrome\Application\chrome.exe`)
driven by Playwright, with the same geometry as the performance smoke:

- `desktop`: desktop 1440x900 dsf 1; 7 cold runs per variant, HTTP cache disabled through CDP, chrome.
- `iphone-13`: iphone-13 390x844 dsf 3, CPU x4 (headless Chrome proxy, not a device); 7 cold runs per variant, HTTP cache disabled through CDP, CPU throttled x4, chrome.
- `iphone-13-webkit`: iphone-13 390x844 dsf 3 on Playwright WebKit (Safari decoder proxy, no CPU throttle); 7 cold runs per variant, no CDP: cache defeated only by `cache: no-store` and unique query strings, webkit.

Each cold run fetches the file with the HTTP cache disabled and a unique query string,
decodes it with `createImageBitmap`, loads it again through Pixi `Assets.load`, uploads it
with `renderer.texture.initSource` and renders one sprite. Numbers are median / p95 over
the runs. **The iPhone-13 column is a viewport, DPR and CPU-throttle proxy measured on this
desktop, not a phone measurement**; decode on a real A15 will differ. GPU memory is
analytic because WebGL exposes no texture memory: RGBA8 is 4 bytes per texel for both PNG
and WebP (the format only changes the wire bytes), 8 bpp UASTC/ASTC/BC7 is 1 byte per texel.

Browser notes:
- none

## Results

| Format | Bytes | Wire ms at 10 Mbps (analytic) | Wire ms at 50 Mbps (analytic) | Delta vs PNG | Encode s | desktop fetch ms (med / p95) | desktop decode ms (med / p95) | desktop Pixi load ms (med / p95) | desktop GPU upload ms (med / p95) | iphone-13 fetch ms (med / p95) | iphone-13 decode ms (med / p95) | iphone-13 Pixi load ms (med / p95) | iphone-13 GPU upload ms (med / p95) | iphone-13-webkit fetch ms (med / p95) | iphone-13-webkit decode ms (med / p95) | iphone-13-webkit Pixi load ms (med / p95) | iphone-13-webkit GPU upload ms (med / p95) | GPU MB at 2048x2048 (analytic) | Fidelity vs shipped PNG |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PNG (shipped) | 3,242,471 | 2,594 | 519 | 0.0% | 0.0 | 13.9 / 15.3 | 26.5 / 29.3 | 39.9 / 45.2 | 10.5 / 14.9 | 16.4 / 28.1 | 33.3 / 38.7 | 43.2 / 48.7 | 36.8 / 44.5 | 27 / 42 | 51 / 68 | 83 / 91 | 11 / 13 | 16.78 MB | byte-identical RGBA |
| WebP lossless (exact) | 2,499,518 | 2,000 | 400 | -22.9% | 9.5 | 11.2 / 21.8 | 45.5 / 50.7 | 58 / 69.1 | 10.5 / 16.6 | 13.4 / 35.4 | 50.4 / 52 | 58.9 / 64.7 | 34.3 / 39.4 | 22 / 39 | 77 / 85 | 106 / 115 | 8 / 9 | 16.78 MB | byte-identical RGBA |
| WebP lossy q90 | 906,980 | 726 | 145 | -72.0% | 14.5 | 6 / 7.3 | 64.9 / 68.8 | 70.2 / 78.7 | 10.2 / 11.7 | 6.7 / 10.3 | 66 / 66.6 | 71.4 / 76.2 | 30.6 / 33.5 | 12 / 33 | 108 / 117 | 118 / 125 | 10 / 14 | 16.78 MB | visible max channel delta 68, mean RGB delta 3.337, alpha changed 0 px |
| KTX2 (UASTC) | not measurable on this host: no encoder installed (install KTX-Software `toktx` and re-run) | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | n/a | 4.19 MB | n/a |

Encode times are one-off build-time costs on this host (PNG is the shipped bytes, no encode).

## 256 px extrapolation

A 256 px frame is 2.56x the area of a 160 px frame. The packed sheet grows by the
same factor, so 648 frames no longer fit one 2048x2048 page: the choice is one
4096x4096 page (67.11 MB decoded RGBA8) or 3 pages of 2048x2048
(50.33 MB RGBA8 in total). Bytes below scale the measured 160 px bytes by the area factor;
treat them as estimates within about 15%.

| Format | Measured 160 px bytes | Estimated 256 px bytes | Fits per-hero cap 3,407,872 | Estimated four-hero total | Fits 12 MiB total |
| --- | --- | --- | --- | --- | --- |
| PNG (shipped) | 3,242,471 | 8,300,726 | no (+143.6% over) | 33,202,904 | no |
| WebP lossless (exact) | 2,499,518 | 6,398,766 | no (+87.8% over) | 25,595,064 | no |
| WebP lossy q90 | 906,980 | 2,321,869 | yes | 9,287,476 | yes |

## The four options

1. **Raise the 12.6 MB (12 MiB) four-hero cap.** Keeps PNG and the PNG-only QA chain. At 256 px
   PNG needs about 8,300,726 bytes per hero and 33,202,904 for four; the per-hero cap
   would have to rise to about 8.5 MiB and the total to about 34 MiB. Only boot is unaffected
   (one hero loads), but each hero switch fetches ~8 MB and cold mobile decode grows with it.
2. **Adopt lossless WebP (exact=True).** -22.9% bytes with byte-identical RGBA, so the
   sourcePixelSha256 / atlasSha256 chain and the two-run reproducibility gates keep their
   meaning. Zero JavaScript. Needs a WebP decode branch in asset QA (`readRgbaPng` is
   PNG-only), the `.png` asserts in the portal flow smoke and production-hero smoke moved
   to the new extension, and the atlas `image` field. At 256 px still over the current
   per-hero cap (6,398,766 estimated), so the cap moves too, to about 6.4 MiB.
3. **Adopt KTX2 (UASTC, transcoded to ASTC/BC7 on device).** The only option that changes GPU
   memory (4.19 MB instead of 16.78 MB at 2048x2048; 16.78 MB instead of
   67.11 MB at 4096x4096). not measurable on this host: no encoder installed (install KTX-Software `toktx` and re-run). Install pointer: https://github.com/KhronosGroup/KTX-Software/releases (toktx --encode uastc) or `basisu`.
   Costs beyond the encoder: about +14 KB in the 1,050,000-byte capped vendor chunk (headroom
   29,941), a self-hosted 930 KB transcoder and worker plumbing under the CSP, block-compression
   artefacts on pixel-art edges that need an art-director pass, and a new QA decode path.
4. **Load one hero at a time.** Already live on the base commit (see Facts). Nothing to build;
   it does not by itself solve the 256 px per-hero cap.

## What the numbers say

- **desktop**: PNG decodes in 26.5 ms median (p95 29.3); WebP lossless (exact) decodes in 45.5 ms (+19.0 ms vs PNG) and completes the Pixi load in 58 ms (+18.1 ms); WebP lossy q90 decodes in 64.9 ms (+38.4 ms vs PNG) and completes the Pixi load in 70.2 ms (+30.3 ms). Fetch from localhost is 13.9 ms for PNG and says nothing about a real link; use the analytic wire columns.
- **iphone-13**: PNG decodes in 33.3 ms median (p95 38.7); WebP lossless (exact) decodes in 50.4 ms (+17.1 ms vs PNG) and completes the Pixi load in 58.9 ms (+15.7 ms); WebP lossy q90 decodes in 66 ms (+32.7 ms vs PNG) and completes the Pixi load in 71.4 ms (+28.2 ms). Fetch from localhost is 16.4 ms for PNG and says nothing about a real link; use the analytic wire columns.
- **iphone-13-webkit**: PNG decodes in 51 ms median (p95 68); WebP lossless (exact) decodes in 77 ms (+26.0 ms vs PNG) and completes the Pixi load in 106 ms (+23.0 ms); WebP lossy q90 decodes in 108 ms (+57.0 ms vs PNG) and completes the Pixi load in 118 ms (+35.0 ms). Fetch from localhost is 27 ms for PNG and says nothing about a real link; use the analytic wire columns.
- On a 10 Mbps link the wire time dominates every decode number above by more than an order of magnitude (PNG 2,594 ms), so smaller bytes win the cold path on real networks even where WebP decodes slower; on localhost or a fast LAN PNG's faster decode wins.

## Recommendation

Carry the roadmap recommendation unless the owner weighs the trade differently: **lossless
WebP with `exact=True` as the committed runtime asset, PNG kept as the Blender intermediate,
per-hero lazy loading kept as is.** It is the only measured option that is both smaller and
pixel-identical, and the wire saving on a real network (about 594 ms
per hero at 10 Mbps) is an order of magnitude larger than its measured decode penalty. Lossy q90
is -72.0% bytes but not pixel-identical (visible max channel delta 68, mean RGB delta 3.337, alpha changed 0 px); the 3x evidence
captures cannot separate it from PNG by eye, so accept it only after a zoomed pixel-diff of the
hero art on a phone, and only with a pinned encoder version because Vercel's Pillow 11.3 and the
host's Pillow 12.3 can carry different libwebp builds (never let the build image encode). KTX2
stays deferred until the owner installs an encoder; if mobile GPU memory becomes the binding
constraint at 4096x4096 it is the option to re-measure first.

Whatever the choice, the 256 px move also needs a decision on **page count** (one 4096x4096
page or three 2048x2048 pages) and a **new per-hero cap**; the memo table gives both numbers.

## If the owner picks WebP (separate cycle, RED first)

1. Asset QA: WebP decode branch beside `readRgbaPng`, same RGBA hash pins.
2. `scripts/smoke-portal-flow.mjs` PNG-header size check and
   `scripts/hmh-reboot-production-hero-browser-smoke.mjs` `.png` response asserts move to
   the new extension; `tests/hmh-reboot-production-hero-atlas.test.mjs` fixture `image` field.
3. Packer (`scripts/build-hmh-blender-atlas.py`) writes the WebP with `exact=True` next to
   the PNG intermediate; `atlasSha256` in the metrics moves to the WebP.
4. URL swap in `apps/hmh-reboot/src/production-hero-atlas.mjs` (0 net JS bytes), then fresh
   certification.

## S-2 status

U-7/S-3 (parent-owned music transport) does **not** cover S-2, so the routing change shipped in
this slice rather than being proven redundant. The child already imported the portal registry
(`combat-audio.mjs` uses `resolveHmhSfxCuePlan` and `resolveHmhSfxVoiceAllocation` against
`HMH_AUDIO_MIX.familyCaps` with `maxVoices 16`), but three gaps remained against the registry
contract, and the first was a live production defect:

1. **Twelve of the fourteen synthesised weapon cues were unregistered and therefore silent.**
   `combat-audio.mjs` refuses any cue absent from `HMH_SFX_CUE_REGISTRY`; only
   `hmh-hash-rail-charge` and `hmh-fire-hash-rail` were registered. The default coin-blaster
   fire, scatter-shotgun, auto-miner, launcher-rig, Lightning Ledger, Bear Market Burner and
   Forked Standard fire cues, reload, dry-fire and the three Lightning interrupt cues returned
   `unknown-cue` in production from 9d669796 (2026-08-05) until this cycle. Reproduced in node
   with a fake `Audio` (12 of 14 refused) before the fix. Fix: the twelve ids are registered in
   `apps/portal/src/hmh-audio-system.mjs` in the existing `weapon` family (cap 16), with
   priorities, cooldowns and fallback tones mirroring their sourced analogues; the registry gate
   itself is kept.
2. **`reduceMotion` was never passed to the cue planner**, so the 0.82 accessibility multiplier
   on impact / boss / damage cues never applied in the child. `setBusLevels(settings)` now
   captures `reduceMotion` from the settings object the child already passes (the pinned call
   stays byte-identical) and `play()` forwards it.
3. **No ducking existed anywhere.** `play()` now derives a 0.7 multiplier for weapon / movement /
   reward / impact / state cues while a `boss`-family voice started less than 600 ms ago; boss,
   damage and ui are never ducked; the window is time-based so a voice that never reports
   `ended` cannot hold the duck.

`status()` gains `unknownCues` (registry refusals) and `reduceMotion`; the child exposes
`dataset.audioUnknownCues` beside `dataset.audioVoices`, and `scripts/hmh-weapon-sfx-browser-smoke.mjs`
now fires the four keyboard weapons in the real child and asserts the counter reads `0`.
Distance attenuation and the `wide` dynamic-range gain are not part of the smallest change
(S-1 / S-4). Projection-only: no simulation, RNG, bridge or save-schema change.

## Evidence

- `.tmp/evidence-atlas-format-and-audio-routing/desktop-chrome-png.png`
- `.tmp/evidence-atlas-format-and-audio-routing/desktop-chrome-webp-lossless.png`
- `.tmp/evidence-atlas-format-and-audio-routing/desktop-chrome-webp-q90.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-chrome-png.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-chrome-webp-lossless.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-chrome-webp-q90.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-webkit-png.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-webkit-webp-lossless.png`
- `.tmp/evidence-atlas-format-and-audio-routing/iphone-13-webkit-webp-q90.png`
- `.tmp/evidence-atlas-format-and-audio-routing/harness/conversion.json`
- `.tmp/evidence-atlas-format-and-audio-routing/harness/measurements.json`
