# S-03 historical recovery record

Superseded by accepted CYCLE-003.md; old open/no-push statements below are historical, not current authority.

# STACKED delivery status

## Accepted baseline
- Branch: `feature/stacked-solo`.
- HEAD at recovery: `15612012fdb1ef52dc013a19dd524ba899035627`.
- S-01 and S-02 are accepted and committed. S-02 implementation: `2300428b0d278ac2e29ef0ce7b4c8f8cddce6ed5`.
- No playable or published STACKED cabinet is claimed.

## Current slice: S-03, incomplete
The original continuous worker exited with code 1 without a final report or checkpoint. Existing S-03 changes remain uncommitted and are preserved as a frozen recovery patch. Its SHA-256 is `c44621c67f6036de2dc9f7f4fc3e04476d0d97b65a977510d10304f4938a0499`.

The log records a roughly four-second gate-command timeout followed by `OSError: [Errno 22] Invalid argument` while writing stdout. The precise reason for the outer CLI exit is not established. An earlier receipt stopped after focused tests, purity, and syntax; it was not treated as a completed gate run.

Recovery verification:
- Parent reran the non-contract focused set: 65 passed, zero failures/skips/todos.
- Fresh full focused set, including contracts: 109 passed, zero failures/skips/todos.
- Fresh purity check passed.
- The supervised Node gate sequence completed, but it did not pass: `vercel:build` failed the attack-table import-isolation test after generating HMH asset metadata. The test mistakes a filename string for an import; parent reproduced the failure and Acorn found zero imports in the flagged module. No generated HMH exemption or threshold relaxation is authorized.
- The ordinary full suite had only the 51 expected retired failures; release, syntax, purity, assets, and repository health gates passed. Outer process exit 0 means the runner finished, not that all gates passed.
- The fresh independent review returned FAIL. Parent reproduced malformed result-tuple acceptance, metadata-sensitive state hashes, attack timestamps missing the required next-tick offset, and a charged attack queue not draining on a defender lock. The review also identified pre-clear REORG scoring order and missing real-runtime terminal/ledger coverage.
- The bounded correction pass implemented issues 1–6 with per-issue RED/GREEN receipts. Parent verified all six changed-file hashes and reran the seven-file focused suite: 72 passed, zero failures/skips/todos.
- Coverage closeout then added reachable terminal/scoring/queue/identity witnesses and reproduced/fixed loss of unapplied attack rows at overflow. Its final seven-file suite passed 79 tests, zero failures/skips/todos; parent verified all eight files listed in the final receipt.
- Contracts §2.3 explicitly makes evidence-ceiling unreachable through legal input. The coverage proof preserves that contract and independently derives the legal byte bound; hostile oversized reassembly rejection is carried forward to S-04. No limits or mutable setup hooks were added.
- The 20-file core candidate is frozen with scoped patch SHA-256 `e942072e5723c2ca40d6de0381acca425f6dfed6ea352226c51573b88ecfad2d`. All implementation writers are stopped. Parent reverified all 20 source hashes and all eight gate-log checksums against the final receipt. No acceptance from earlier failed receipts or the implementer's report.
- All eight required Node gates passed under the established baseline exception, including deployment build, assets and strict repository health. Core/contract tests: 123/123 passed. Full npm test: 2,675 tests, 2,624 passed, exactly 51 expected retired failures, no new failure names, zero skipped/todo. Its raw exit remains 1; this is baseline-aware acceptance of the test gate, not a claim of an entirely green suite.
- The final Codex review was blocked by its read-only sandbox rejecting local hash commands; it did not establish a code-review verdict. Its wrapper was explicitly stopped without changing security policy. The replacement batch covered tuple/hash/API integrity, simulation/scoring/terminals, and match queues/import isolation, but all three reviewers reached their 600-second timeout without a verdict. One transcript also records a denied command, which must not be retried or bypassed. Parent rechecked all 20 frozen files, the patch hash/length and HEAD: unchanged. Partial notes and test reruns are not independent acceptance. The owner then approved a stronger self-contained source-only review. That review received a parent-hash-verified packet with shell execution disabled for the invocation and unchanged read-only security policy. Its completion and reconciliation are recorded below.
- The authorized stronger source-only review completed with blocking findings. Parent audited zero tool/command events and verified the 20 original frozen file hashes. Real probes confirmed an incomplete future-affecting state hash, impossible hold counts, missing tick-ceiling identity, and oversized attack tables throwing after partial match advancement. A golden-hash version bump is also required. Seed normalization and rejection-sampling findings conflict with explicit rules; the perfect-clear cap matches the authoritative contract and is not changed without a reachable counterexample.
- Ten new behavioral regressions now fail for these confirmed gaps. The earlier 8/8 gate result remains historical evidence for the original candidate, not acceptance of this newly exposed state. A separate command-free corrective agent is producing scoped text proposals; only the parent may apply and test them. No new candidate is accepted or committed.
- Corrective proposals were applied by the parent, followed by three additional RED/GREEN parser guards for sparse numeric arrays, sparse qualifiers and oversized version strings. The version identity gate was updated without removing its exact checks. The final nine-file focused run passes **136/136**, including **13** new regressions, with zero failures/skips/todos. Purity and syntax also passed before the final guard refinement; full gates are rerunning on the exact final source.
- Cabinet version **0.2.0** records the deliberate SRT2/SMH2 canonical-hash correction in contracts §4.3.1 and versus §2.7. Actual golden replay preserves its result tuple and moves state hash `3232793281` → `1015998860`. Seed normalization, RNG rejection sampling and gameplay tuning remain unchanged.
- A new 23-file candidate is frozen at patch SHA-256 `c7624d340d5ea1f1b5c443800ad79de9cc2fc22a712eb390349ba07560329710`. Its static scan has zero secret/eval/shell-injection/private-path matches. All eight fresh Node child gates passed baseline-aware: 136 focused passes; full suite 2,688 tests / 2,637 passes / exactly 51 inherited retired failures, no new failure names, zero skips/todos. Parent verified all eight log hashes and all 23 frozen source hashes plus the exact patch. Raw npm test remains exit 1 for the known baseline. The fresh command-free independent review is still running; no acceptance, commit or S-04 advancement yet.
- R2's source-only review completed with three additional blockers: template-literal import edges were missed by the tree audit, the new regression file was absent from the syntax allowlist, and length-valid malformed metadata could reach terminal then make `result()` throw. Parent confirmed all three in a 24-test RED run (19 passed, five failed). Existing application modules have no unresolved non-literal module references, so the guard can fail closed without legacy exemptions. A final bounded corrective proposal is running command-free; R2 gates are now historical evidence, not acceptance of R3. After this second complete correction/reverification cycle, remaining blockers must be escalated rather than silently looping.
- Final bounded correction: constructors now use the exact tuple metadata patterns before creating state; the syntax list includes the new regression file; static string/template/concatenation imports are resolved without execution, unknown imports fail closed, and URI-encoded target spellings are covered by a real app-fixture RED/GREEN test. All **140** focused tests now pass, zero failures/skips/todos. A fresh 23-file candidate is frozen at `14b84f2a440f80a15f7b5f0925e68383968ded83f9d52a4ef372361e11dd2cd6`; only five files differ from R2, with 18 byte-identical. Fresh full Node gates and an independent source-only closure review of the three blockers plus the five-file delta are running. Acceptance must compose that closure with R2's unchanged-code coverage and actual fresh gates; no standalone/global pass is implied.
- R3 full verification finished: all eight Node gates passed baseline-aware; 140 focused tests; full suite **2,692 total / 2,641 passed / 51 inherited retired failures**, zero new failure names or skips/todos. Parent verified eight log checksums and all 23 frozen files plus the exact patch.
- The final command-free closure review closed metadata and syntax registration but retained **one import-isolation blocker**: the tree guard skips every app filename ending in `stacked-versus-table.mjs`. Parent reproduced the bypass using the exact extracted tree-guard functions and a same-named nested module containing a real import edge; the guard missed it. All temporary fixtures were removed, and the 23 frozen source files and patch remained unchanged. No review/implementation worker is running. The bounded two-round correction policy now requires escalation before removing that exemption and doing one further verification round. S-03 is not accepted or committed.
- S-03 remains unaccepted and uncommitted. No S-04 continuation is authorized until it is closed.

The next implementation slice must not begin until S-03 findings are resolved, the exact source passes all required gates, and acceptance evidence is recorded.

## Other work
- Portal S-06 candidate: integration and complete acceptance pending.
- Renderer S-11/S-12: parent rebuilt and verified actual desktop/mobile compositor pixels under strict CSP, not merely canvas initialization. Reset resurrection and orphaned descendant disposal were reproduced with two failing tests and fixed; 65 focused tests and fresh two-profile browser audit pass. Independent lifecycle-only review passed with no findings; parent rechecked both exact file hashes before recording the verdict. A separate direct CSS-viewport layout containment failure remains (320×568 solo and 900×500 two-board); current root-fitted fixture screenshots do not certify that public layout contract. No S-12 acceptance or integration yet.
- ChatGPT-generated Free and Ranked menu-art candidates exist with preserved originals, prompts, checksum manifest and optimized derivatives. They are not integrated menus. Cabinet/title, loading/results and remaining requested artwork are unfinished.
- S-04 onward remains pending except for separately prepared candidates. Do not convert a preparation artifact into accepted cycle status.

## Boundaries
Keep browser gates serial and separate from the Node-only deployment build. Preserve HMH/Chikun source, saves and shared systems. No production promotion, paid entry, settlement, contract deployment, authority changes or real-money actions are authorized. Ranked remains a local replay-verified prototype until its required integrity and isolation gates pass.

Operational process IDs, receipt paths and artwork locations are in the ignored `.tmp/stacked-continuation-status.json` checkpoint.
