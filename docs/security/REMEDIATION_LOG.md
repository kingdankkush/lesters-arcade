# Security Remediation Log

Source handoff: `docs/security/lesters-arcade-security-remediation-handoff.md` copied from `Downloads/lesters-arcade-security-remediation-handoff.md`.

Canonical source review status: **missing at start**. The handoff references `lesters-arcade-security-review-2026-07-07.md` at audit commit `1b111fe3`, but that file was not present in `Downloads/` or `docs/security/` when remediation began. Current verdicts below were re-located against current HEAD source files.

Current verification note, 2026-07-08: the security hardening changes are committed in `864c0337` unless a row says otherwise. This host does not currently have Foundry `forge` installed, so `npm run contracts:test` cannot be rerun here; latest locally rerun gates for this log cleanup were `node --test tests/contracts-security-abi.test.mjs`, `npm test`, `npm run check`, `npm run contracts:check`, and `npm run vercel:build`.

| Finding | WO | Current HEAD verdict | Evidence at current HEAD | Fix commit | Test / gate | Status |
|---|---:|---|---|---|---|---|
| CRITICAL-1 | 116 | Fixed and committed | `SessionLedger.closeSession()` requires `msg.sender == sess.player`, requires `exists && playable`, and recovers via OZ `ECDSA.recover` against `GameRegistry.getGame(sess.gameId).devWallet`. | `864c0337` | `SecurityBaseline.t.sol::testNonPlayerCannotCloseSessionEvenWithValidSignature`, `testPlayerCannotCloseSessionWithRandomSigner`; current host lacks `forge` rerun | fixed |
| HIGH-3 | 116 | Fixed and committed | `SessionLedger.openSession()` uses `playerNonces[msg.sender]` in `keccak256(chainid, this, player, gameId, nonce)` and records `openedAt`. | `864c0337` | Foundry structural/session suite recorded; current host lacks `forge` rerun | fixed |
| CRITICAL-2 | 117 | Fixed and committed | `PaymentRouter.setDefaultVaults()` is `onlyOperator`; constructor requires nonzero operator. | `864c0337` | `SecurityBaseline.t.sol::testUnauthorizedSetDefaultVaultsReverts`; current host lacks `forge` rerun | fixed |
| HIGH-2 | 117 | Fixed and committed | `PaymentRouter` constructor no longer takes `_sessionLedger`; one-time `setSessionLedger()` is operator-gated. | `864c0337` | compile/deploy fixture coverage recorded; current host lacks `forge` rerun | fixed |
| LOW-1 | 117 | Fixed and committed | `PaymentRouter.splitAndDisburse()` requires all default vaults to be nonzero before transfer. | `864c0337` | router suite recorded; current host lacks `forge` rerun | fixed |
| CRITICAL-3 | 118 | Fixed and committed | `ArcadePaymentRouter.startPaidSession()` no longer accepts caller-controlled token or split config; token/splits are registry/operator-derived and the function is disabled by default while ranked is free. | `864c0337` | `tests/contracts-security-abi.test.mjs`; `SecurityBaseline.t.sol::testArcadePaymentRouterPaidSessionsAreDisabledByDefault` recorded | fixed |
| CRITICAL-4 | 119 | Partially fixed and committed | `SessionLedger.settle()` transfers to router, calls `splitAndDisburse(sess.gameId, sess.player, sess.entryFee)`, and uses `nonReentrant`. A fresh atomic settlement balance invariant still needs a Foundry-capable host. | `864c0337` | planned atomic split invariant | partial |
| HIGH-1 | 119/131 | Partially fixed and committed | Path A settlement is atomic in code, but deployment/wiring remains blocked by WO-115/WO-131 HALTs. | `864c0337` | planned settlement balance test | blocked |
| HIGH-4 | 120 | Partially fixed and committed | `ScoreSubmissionRegistry.submitSession()` requires registered/playable game, plausibility bounds, duplicate-session rejection, and achievement cap. Verifier attestation path and generated `IntegrityBounds.sol` are still pending. | `864c0337` | `SecurityBaseline.t.sol::testScoreSubmissionRejectsOutOfBoundsScore`, `testScoreSubmissionRejectsUnplayableGameAndTooManyAchievements` recorded; current host lacks `forge` rerun | partial |
| MEDIUM-1 | 116/120/122 | Fixed and committed for current call sites | `SessionLedger`, `ScoreSubmissionRegistry`, and `ArcadePaymentRouter` consume `exists && playable`; `GameRegistry.setPlayable(true)` requires dev-wallet confirmation. | `864c0337` | `SecurityBaseline.t.sol::testGameCannotBecomePlayableBeforeDevWalletConfirms`, score/router tests recorded | fixed |
| HIGH-5 | 121 | Fixed and committed | `scripts/deploy-contracts.mjs` no longer requires, loads, deploys, or auto-patches `LestersArcadeCore`; the canonical path deploys only the seven module suite. | `30ec933e` | `tests/deploy-contracts-security.test.mjs` | fixed |
| MEDIUM-2 | 122 | Fixed and committed | `GameRegistry.confirmDevWallet(gameId)` is callable only by the registered dev wallet, and `setPlayable(true)` requires confirmation. | `864c0337` | `SecurityBaseline.t.sol::testGameCannotBecomePlayableBeforeDevWalletConfirms` recorded | fixed |
| MEDIUM-3 | 123 | Fixed and committed | `PlayerProfileRegistry` normalizes handle uniqueness and enforces username length/charset/moderation bounds through the same rules tested in JS username handling. | `864c0337` | username + profile registry tests recorded | fixed |
| MEDIUM-4 | 124 | Fixed and committed | `TournamentPool` has owner-only `withdrawTournamentPrize()` with non-zero recipient/amount checks, prize-pool accounting, and a withdrawal event. | `864c0337` | `SecurityBaseline.t.sol::testTournamentPrizePoolCanBeWithdrawnByOwner` recorded | fixed |
| MEDIUM-5 | 117/122/124 | Partially fixed and committed | `PaymentRouter` and `GameRegistry` use pending/accept operator transfer. `TournamentPool.owner` is immutable for the current shell; full two-step tournament admin rotation remains future work. | `864c0337` | contract structure + Foundry tests recorded | partial/future |
| MEDIUM-10 | 125 | Fixed and committed, scanner rerun pending on equipped host | Foundry config and Slither config are present. Current host lacks `forge`; Slither rerun should be done on the security workstation/CI image that has the toolchain. | `864c0337` | `npm run contracts:test`, `npm run contracts:slither` | toolchain-blocked |
| HIGH-6 | 115/131 | Still present | Two competing settlement systems remain in-tree without a recorded canonical deployment choice. | pending | architecture doc + Justin ruling | HALT pending |
| HIGH-8 | 115/128 | Fixed and committed, copy still subject to WO-115 approval | `buildLeaderboardModel()` and the visible leaderboard panel disclose testnet-beta player-submitted scores, no value attached, and reset risk. | `864c0337` | `arcade-core.test.mjs` leaderboard disclosure assertions | fixed/approval-gated |
| HIGH-7 | 126 | Partially fixed and committed | `settlement.mjs` generic live path is dev-flagged; production writes should use `litvm-chain-client.mjs`. Byte-equal client/planner ABI fixture is still pending. | `864c0337` | `settlement.test.mjs` generic-live disabled/chain guard tests | partial |
| MEDIUM-6 | 127 | Fixed and committed | `recordCadenceScore()` and flat leaderboard writes dedupe by `sessionId`. | `864c0337` | `leaderboard-engine.test.mjs` duplicate-session cadence test, `arcade-core.test.mjs` retry test | fixed |
| MEDIUM-7 | 127 | Fixed and committed | `applySettlement()` replaces existing settlement rows for a repeated `sessionId` instead of appending duplicates. | `864c0337` | `arcade-core.test.mjs::applySettlement dedupes settlement retries by sessionId` | fixed |
| MEDIUM-8 | 126 | Fixed and committed for generic path | Generic injected transaction settlement requires explicit dev opt-in and a fresh chain-id check before every broadcast. | `864c0337` | `settlement.test.mjs` chain guard tests | fixed |
| MEDIUM-9 | 129 | Fixed and committed for SDK/first-party cabinet | SDK adds `parsePostMessageEvent()` source/origin gate; HMH cabinet captures parent origin and avoids wildcard post once known; docs describe two-layer validation. | `864c0337` | `arcade-sdk.test.mjs` spoofed-source/origin tests | fixed |
| MEDIUM-11 | 130 | Fixed and committed, log-grep test still pending | `deploy-contracts.mjs` no longer prints a private-key fragment before wallet initialization; it prints deployer address only after constructing the wallet. | `864c0337` | planned log grep test | fixed/pending-extra-test |
| LOW-2 | 130 | Open | License ruling required: proprietary LICENSE vs SDK license. | pending | pending Justin ruling | HALT pending |
| LOW-3 | 130 | Fixed and committed | Simulated settlements store `primaryTxHash: null` and `primarySimulatedTxHash: sim:<hash>`; leaderboard rows carry simulated hash separately. | `864c0337` | `settlement.test.mjs`, `arcade-core.test.mjs` simulated receipt assertions | fixed |
| LOW-4 | 130 | Fixed, committed, and deployed | Production Vercel headers now ship a reviewed CSP: remote executable scripts and remote stylesheets are blocked; inline style/script remains explicitly allowed only for the current static shell bootstrap; object/embed and frame ancestors are blocked; LitVM RPC/WebSocket connections are allowlisted. | `dfa76c7e` | `tests/vercel-security-headers.test.mjs`, `npm run vercel:build`, production header live-check | fixed |
| LOW-5 | 130/122 | Fixed and committed locally; no contract transaction sent | Cabinet manifests now derive a canonical load-bearing payload, keccak-256 `manifestChecksum`, and `manifestRegistryAnchor()` with `gameId = keccak256(idString)` matching `GameRegistry.registerGame()`. This creates the bytes32 anchor values for future registry recording without requiring a contract deployment in this session. | `30ec933e` | `tests/game-manifest.test.mjs`, `npm run vercel:build` | fixed/local-anchor |
