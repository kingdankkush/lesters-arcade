# Security Remediation Log

Source handoff: `docs/security/lesters-arcade-security-remediation-handoff.md` copied from `Downloads/lesters-arcade-security-remediation-handoff.md`.

Canonical source review status: **missing at start**. The handoff references `lesters-arcade-security-review-2026-07-07.md` at audit commit `1b111fe3`, but that file was not present in `Downloads/` or `docs/security/` when remediation began. Current verdicts below were re-located against current HEAD source files.

| Finding | WO | Current HEAD verdict | Evidence at current HEAD | Fix commit | Test / gate | Status |
|---|---:|---|---|---|---|---|
| CRITICAL-1 | 116 | Fixed in working tree | `SessionLedger.closeSession()` now requires `msg.sender == sess.player`, requires `exists && playable`, and recovers via OZ `ECDSA.recover` against `GameRegistry.getGame(sess.gameId).devWallet`. | pending commit | `SecurityBaseline.t.sol::testNonPlayerCannotCloseSessionEvenWithValidSignature`, `testPlayerCannotCloseSessionWithRandomSigner` | green |
| HIGH-3 | 116 | Fixed in working tree | `SessionLedger.openSession()` now uses `playerNonces[msg.sender]` in `keccak256(chainid, this, player, gameId, nonce)` and records `openedAt`. | pending commit | `contracts:test` structural/session suite | green |
| CRITICAL-2 | 117 | Fixed in working tree | `PaymentRouter.setDefaultVaults()` is now `onlyOperator`; constructor requires nonzero operator. | pending commit | `SecurityBaseline.t.sol::testUnauthorizedSetDefaultVaultsReverts` | green |
| HIGH-2 | 117 | Fixed in working tree | `PaymentRouter` constructor no longer takes `_sessionLedger`; one-time `setSessionLedger()` is operator-gated. | pending commit | `contracts:test` compile/deploy fixture coverage | green |
| LOW-1 | 117 | Fixed in working tree | `PaymentRouter.splitAndDisburse()` now requires all default vaults to be nonzero before transfer. | pending commit | `contracts:test` router suite | green |
| CRITICAL-3 | 118 | Fixed in working tree | `ArcadePaymentRouter.startPaidSession()` no longer accepts caller-controlled token or split config; token/splits are registry/operator-derived and the function is disabled by default while ranked is free. | pending commit | `tests/contracts-security-abi.test.mjs`, `SecurityBaseline.t.sol::testArcadePaymentRouterPaidSessionsAreDisabledByDefault` | green |
| CRITICAL-4 | 119 | Fixed in working tree | `SessionLedger.settle()` now transfers to router, calls `splitAndDisburse(sess.gameId, sess.player, sess.entryFee)`, and uses `nonReentrant`. | pending commit | planned atomic split invariant | in progress |
| HIGH-1 | 119/131 | Partially fixed in working tree | Path A settlement is now atomic in code, but deployment/wiring remains blocked by WO-115/WO-131 HALTs. | pending commit | planned settlement balance test | in progress |
| HIGH-4 | 120 | Partially fixed in working tree | `ScoreSubmissionRegistry.submitSession()` now requires registered/playable game, plausibility bounds, duplicate-session rejection, and achievement cap. Verifier attestation path and generated `IntegrityBounds.sol` are still pending. | pending commit | `SecurityBaseline.t.sol::testScoreSubmissionRejectsOutOfBoundsScore`, `testScoreSubmissionRejectsUnplayableGameAndTooManyAchievements` | green/in progress |
| MEDIUM-1 | 116/120/122 | Fixed for current call sites in working tree | `SessionLedger`, `ScoreSubmissionRegistry`, and `ArcadePaymentRouter` consume `exists && playable`; `GameRegistry.setPlayable(true)` now requires dev-wallet confirmation. | pending commit | `SecurityBaseline.t.sol::testGameCannotBecomePlayableBeforeDevWalletConfirms`, score/router tests | green |
| HIGH-5 | 121 | Still present | `LestersArcadeCore` deploys a wrapper composition that mis-wires multiple modules and should not be canonical deployment. | pending | planned deploy artifact exclusion test | open |
| MEDIUM-2 | 122 | Fixed in working tree | `GameRegistry.confirmDevWallet(gameId)` is callable only by the registered dev wallet, and `setPlayable(true)` requires confirmation. | pending commit | `SecurityBaseline.t.sol::testGameCannotBecomePlayableBeforeDevWalletConfirms` | green |
| MEDIUM-3 | 123 | Fixed in working tree | `PlayerProfileRegistry` now normalizes handle uniqueness and enforces username length/charset/moderation bounds through the same rules tested in JS username handling. | pending commit | username + profile registry tests | green |
| MEDIUM-4 | 124 | Fixed in working tree | `TournamentPool` now has owner-only `withdrawTournamentPrize()` with non-zero recipient/amount checks, prize-pool accounting, and a withdrawal event. | pending commit | `SecurityBaseline.t.sol::testTournamentPrizePoolCanBeWithdrawnByOwner` | green |
| MEDIUM-5 | 117/122/124 | Partially fixed in working tree | `PaymentRouter` and `GameRegistry` now use pending/accept operator transfer. `TournamentPool.owner` is immutable for the current shell; full two-step tournament admin rotation remains future work. | pending commit | contract structure + Foundry tests | green/future |
| MEDIUM-10 | 125 | Fixed in working tree | Foundry is configured and Slither is wired with `slither.config.json --fail-high`; the current scanner run reports `0 result(s) found`. | pending commit | `npm run contracts:test`, `npm run contracts:slither` | green |
| HIGH-6 | 115/131 | Still present | Two competing settlement systems remain in-tree without a recorded canonical deployment choice. | pending | architecture doc + Justin ruling | HALT pending |
| HIGH-8 | 115/128 | Fixed in working tree, copy still subject to WO-115 approval | `buildLeaderboardModel()` and the visible leaderboard panel now disclose testnet-beta player-submitted scores, no value attached, and reset risk. | pending commit | `arcade-core.test.mjs` leaderboard disclosure assertions | green |
| HIGH-7 | 126 | Partially fixed in working tree | `settlement.mjs` generic live path is dev-flagged; production writes should use `litvm-chain-client.mjs`. Byte-equal client/planner ABI fixture still pending. | pending commit | `settlement.test.mjs` generic-live disabled/chain guard tests | green/in progress |
| MEDIUM-6 | 127 | Fixed in working tree | `recordCadenceScore()` and flat leaderboard writes dedupe by `sessionId`. | pending commit | `leaderboard-engine.test.mjs` duplicate-session cadence test, `arcade-core.test.mjs` retry test | green |
| MEDIUM-7 | 127 | Fixed in working tree | `applySettlement()` replaces existing settlement rows for a repeated `sessionId` instead of appending duplicates. | pending commit | `arcade-core.test.mjs::applySettlement dedupes settlement retries by sessionId` | green |
| MEDIUM-8 | 126 | Fixed in working tree for generic path | Generic injected transaction settlement requires explicit dev opt-in and a fresh chain-id check before each broadcast. | pending commit | `settlement.test.mjs` chain guard tests | green |
| MEDIUM-9 | 129 | Fixed in working tree for SDK/first-party cabinet | SDK adds `parsePostMessageEvent()` source/origin gate; HMH cabinet captures parent origin and avoids wildcard post once known; docs describe two-layer validation. | pending commit | `arcade-sdk.test.mjs` spoofed-source/origin tests | green |
| MEDIUM-11 | 130 | Fixed in working tree | `deploy-contracts.mjs` no longer prints a private-key fragment before wallet initialization; it prints deployer address only after constructing the wallet. | pending commit | planned log grep test | in progress |
| LOW-2 | 130 | Open | License ruling required: proprietary LICENSE vs SDK license. | pending | pending Justin ruling | HALT pending |
| LOW-3 | 130 | Fixed in working tree | Simulated settlements now store `primaryTxHash: null` and `primarySimulatedTxHash: sim:<hash>`; leaderboard rows carry simulated hash separately. | pending commit | `settlement.test.mjs`, `arcade-core.test.mjs` simulated receipt assertions | green |
| LOW-4 | 130 | Needs verification | Sandbox CSP still needs style-src review. | pending | planned CSP test | open |
| LOW-5 | 130/122 | Needs verification | Manifest checksum should be cryptographic keccak and registry-anchored. | pending | planned digest test | open |
