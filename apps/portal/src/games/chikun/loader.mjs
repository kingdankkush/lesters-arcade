// Chikun — Cabinet SDK v1 vertical slice loader.
// Kept at src/games/chikun/loader.mjs for the existing lazy-load convention,
// but it now delegates to the real vertical-slice module instead of returning a
// placeholder stub.

export { loadChikunGame } from '../../chikun-cabinet.mjs';
